{
  "name": "webhot",
  "version": "0.1.0",
  "private": true,
  "description": "WebHot — AI Agent 实时世界状态系统",
  "scripts": {
    "dev": "corepack pnpm --filter @webhot/api --filter @webhot/mcp --filter @webhot/worker --filter @webhot/webhook-worker dev",
    "dev:api": "corepack pnpm --filter @webhot/api dev",
    "dev:mcp": "corepack pnpm --filter @webhot/mcp dev",
    "dev:worker": "corepack pnpm --filter @webhot/worker dev",
    "dev:webhook": "corepack pnpm --filter @webhot/webhook-worker dev",
    "build": "corepack pnpm -r build",
    "lint": "eslint . --ext .ts",
    "typecheck": "corepack pnpm -r typecheck",
    "clean": "corepack pnpm -r clean",
    "setup": "corepack pnpm install && corepack pnpm build"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
