{
  "name": "@webhot/perception",
  "version": "0.1.0",
  "private": true,
  "description": "WebHot Perception Engine — 感知引擎 (Snapshot + Diff)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@webhot/schemas": "workspace:*",
    "@webhot/storage": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
