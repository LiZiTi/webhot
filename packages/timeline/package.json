{
  "name": "@webhot/timeline",
  "version": "0.1.0",
  "private": true,
  "description": "WebHot Timeline Engine — 时间线引擎",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "clean": "rm -rf dist" },
  "dependencies": { "@webhot/schemas": "workspace:*", "@webhot/storage": "workspace:*" },
  "devDependencies": { "typescript": "^5.7.0" }
}
