{
  "name": "@webhot/core",
  "version": "0.1.0",
  "private": true,
  "description": "WebHot 核心业务逻辑",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@webhot/schemas": "workspace:*",
    "@webhot/storage": "workspace:*",
    "@webhot/ranking": "workspace:*",
    "@webhot/semantic": "workspace:*",
    "@webhot/signal": "workspace:*",
    "@webhot/timeline": "workspace:*",
    "@webhot/perception": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
